server:
  port: 8086
spring:
  servlet:
    multipart:
      max-file-size: 10GB
      max-request-size: 500MB
  config:
    import: classpath:Firebase.env[.properties]
    
upload:
  chunk:
    file:
      extension: .part
  dir:
    upload: test-uploads
    chunk: test-temp-uploads

